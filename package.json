{
  "name": "binokio",
  "private": true,
  "description": "Run API + Vite together for local development",
  "scripts": {
    "dev": "concurrently -k -n api,web -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
