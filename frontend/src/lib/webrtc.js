/** Free STUN servers for WebRTC (no paid TURN — may fail on some strict networks). */
export const ICE_SERVERS = {
  iceServers: [
    { urls: 'stun:stun.l.google.com:19302' },
    { urls: 'stun:stun1.l.google.com:19302' }
  ]
};
